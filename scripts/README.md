# Scripts

This directory stores GitHub-safe helper scripts.

These scripts should:

- avoid private data dependencies
- avoid workstation-specific hard-coded paths
- avoid internal operational secrets
- help readers validate or inspect the public example bundles
